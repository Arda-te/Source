-- ARDA HUB V11 - MOBİL GUI TEST

local Players = game:GetService("Players")
local Player = Players.LocalPlayer

local Gui = Instance.new("ScreenGui")
Gui.Name = "ArdaHubV11"
Gui.ResetOnSpawn = false
Gui.IgnoreGuiInset = true
Gui.DisplayOrder = 999999
Gui.Parent = Player:WaitForChild("PlayerGui")

-- Aç/Kapat
local Open = Instance.new("TextButton")
Open.Size = UDim2.fromOffset(65,65)
Open.Position = UDim2.new(1,-80,0,25)
Open.BackgroundColor3 = Color3.fromRGB(180,0,30)
Open.Text = "☰"
Open.TextColor3 = Color3.new(1,1,1)
Open.TextSize = 30
Open.Font = Enum.Font.GothamBlack
Open.ZIndex = 100
Open.Parent = Gui

local oc = Instance.new("UICorner")
oc.CornerRadius = UDim.new(1,0)
oc.Parent = Open

-- Panel
local Panel = Instance.new("Frame")
Panel.Size = UDim2.new(0.86,0,0.72,0)
Panel.Position = UDim2.new(0.07,0,0.14,0)
Panel.BackgroundColor3 = Color3.fromRGB(12,12,18)
Panel.BorderSizePixel = 0
Panel.ZIndex = 10
Panel.Parent = Gui

local pc = Instance.new("UICorner")
pc.CornerRadius = UDim.new(0,20)
pc.Parent = Panel

local ps = Instance.new("UIStroke")
ps.Color = Color3.fromRGB(255,20,45)
ps.Thickness = 3
ps.Parent = Panel

-- Başlık
local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1,0,0,65)
Title.BackgroundColor3 = Color3.fromRGB(150,0,25)
Title.Text = "ARDA HUB V11"
Title.TextColor3 = Color3.new(1,1,1)
Title.TextSize = 25
Title.Font = Enum.Font.GothamBlack
Title.ZIndex = 11
Title.Parent = Panel

local tc = Instance.new("UICorner")
tc.CornerRadius = UDim.new(0,20)
tc.Parent = Title

-- Buton alanı
local List = Instance.new("ScrollingFrame")
List.Position = UDim2.fromOffset(10,75)
List.Size = UDim2.new(1,-20,1,-85)
List.BackgroundTransparency = 1
List.BorderSizePixel = 0
List.ScrollBarThickness = 5
List.ZIndex = 11
List.Parent = Panel

local Layout = Instance.new("UIListLayout")
Layout.Padding = UDim.new(0,8)
Layout.Parent = List

local function Button(text)

	local B = Instance.new("TextButton")

	B.Size = UDim2.new(1,-5,0,55)
	B.BackgroundColor3 = Color3.fromRGB(35,35,45)
	B.BorderSizePixel = 0
	B.Text = text.."  •  KAPALI"
	B.TextColor3 = Color3.new(1,1,1)
	B.TextSize = 16
	B.Font = Enum.Font.GothamBold
	B.ZIndex = 12
	B.Parent = List

	local C = Instance.new("UICorner")
	C.CornerRadius = UDim.new(0,12)
	C.Parent = B

	B.Activated:Connect(function()

		if B:GetAttribute("Enabled") then

			B:SetAttribute("Enabled",false)
			B.Text = text.."  •  KAPALI"
			B.BackgroundColor3 =
				Color3.fromRGB(35,35,45)

		else

			B:SetAttribute("Enabled",true)
			B.Text = text.."  •  AÇIK"
			B.BackgroundColor3 =
				Color3.fromRGB(150,0,25)

		end

	end)

	return B
end

Button("❤️ Ölümsüzlük")
Button("🚀 Mobil Fly")
Button("⚡ Süper Hız")
Button("🦘 Yüksek Zıplama")
Button("♾️ Sonsuz Zıplama")
Button("👻 Noclip")
Button("🛑 Hover")
Button("☀️ FullBright")
Button("🏃 Sprint")
Button("🔄 Respawn")

Layout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
	List.CanvasSize =
		UDim2.fromOffset(
			0,
			Layout.AbsoluteContentSize.Y + 15
		)
end)

local visible = true

Open.Activated:Connect(function()

	visible = not visible
	Panel.Visible = visible

	if visible then
		Open.Text = "×"
	else
		Open.Text = "☰"
	end

end)

Open.Text = "×"
